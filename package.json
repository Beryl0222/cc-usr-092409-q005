{
  "name": "neighborhood-classroom-safeguarding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "领域事件基础校验",
  "scripts": {
    "test": "node --test",
    "build": "for f in src/*.js scripts/*.mjs; do node --check \"$f\" || exit 1; done",
    "sample": "node scripts/build-sample.mjs"
  }
}
